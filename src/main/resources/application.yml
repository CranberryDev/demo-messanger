server:
  port: 8081

spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
  datasource:
    url: jdbc:postgresql://localhost:5432/demo_project
    username: postgres
    password: postgres
    hikari:
      schema: demo_schema
  security:
    user:
      password: password
      name: user